server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/user_db
    username: Sakshi
    password: Sakshi
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect


eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8087/eureka
  instance:
    prefer-ip-address: true